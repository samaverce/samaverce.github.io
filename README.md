# Samaverce one-page website

This folder is ready for GitHub Pages. Upload its contents to a GitHub repository, then in the repository settings enable **Pages** from the `main` branch and the root folder.

The site is a single responsive `index.html` file. Contact information and company description are drawn from the supplied public Facebook page.
